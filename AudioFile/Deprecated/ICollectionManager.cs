/*public interface ICollectionManager<T>
{
    void Add(T item);
    T Remove();
    bool IsEmpty();
    int Count { get; }
    T Get(int index); // Retrieve an item by index
    void Set(int index, T item); // Set an item at a specific index
    MediaItem GetSelection();
    int GetSelectionCount();
    void ClearSelection(); //good idea to have this method defined here
    List<MediaItem> GetItems();
    int GetItemCount();
    List<MediaItem> GetSelectedItems();
    int GetSelectedItemCount();
    void Clear(); //good idea to have this method
    void SelectAll();
    void DeselectAll();
    MediaItem GetItem(int index);
    MediaItem GetSelectedItem(int index);
    void SelectItem(int index);
    void DeselectItem(int index);
    bool IsItemSelected(int index);
}
*/